const SERVER_PORT = 3000;

const WORLD_SIZE = {
  WIDTH: 2609,
  HEIGHT: 905,
};

const VIEWPORT_SIZE = {
  WIDTH: 1280,
  HEIGHT: 720,
};

const PLAYER_SIZE = {
  WIDTH: 30,
  HEIGHT: 40,
};

module.exports = {
  SERVER_PORT,
  VIEWPORT_SIZE,
  WORLD_SIZE,
  PLAYER_SIZE,
};
